<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="BlackDog.Views.Menu.Template.MenuTemplate">
  <ContentView.Content BackgroundColor="{Binding BackgroundColor}">
    <Grid Padding="20,14,0,6">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!--<Image Grid.Column="0" Grid.Row="0" Source="{Binding Icon}"   HeightRequest="25" WidthRequest="25"/>-->
      <Label Grid.Column="0" Grid.Row="0" Text="{Binding Title}" Style="{DynamicResource MenuStyle}"/>
      <!--<Switch Grid.Column="2" Grid.Row="0" />-->
    </Grid>
  </ContentView.Content>
</ContentView>
